---
- name: Install oh-my-zsh
  ansible.builtin.git:
    repo: https://github.com/ohmyzsh/ohmyzsh.git
    dest: "{{ dotfiles_oh_my_zsh_dir }}"
    version: master
    update: "{{ dotfiles_mode == 'update' }}"
  when: dotfiles_oh_my_zsh_enabled
