# Extra official-repo packages installed by `install`. [D-SCRIPTS-MINIMAL][F-CLI]
# One package per line; '#' comments and blank lines are ignored.

# Dev toolchain — used by `make ci` / `make fmt`. Pin to pacman (not mise's go
# install dir, which mise doesn't shim) so they're on PATH unconditionally —
# including in non-interactive shells where `mise activate` isn't loaded.
shellcheck
shfmt

git-delta              # syntax-highlighted git diffs (binary: delta)
wireguard-tools        # wg-quick — used by vpns() / exitnode() in shell.local.sh

# Selected Omarchy defaults removed by "Remove Preinstalled"; restore them
# through pacman, not alternate launchers or self-managed installers. [D-PKG-REMOVE]
opencode               # OpenCode; install seeds Omarchy config only when missing.
obsidian               # workspace 3 — notes
discord                # chat (native client; omarchy default is a webapp — we want full voice/screen-share)
