# Extra official-repo packages installed by `install`. [D-SCRIPTS-MINIMAL][F-CLI]
# One package per line; '#' comments and blank lines are ignored.

# Dev toolchain — used by `make ci` / `make fmt`. Pin to pacman (not mise's go
# install dir, which mise doesn't shim) so they're on PATH unconditionally —
# including in non-interactive shells where `mise activate` isn't loaded.
shellcheck
shfmt

git-delta              # syntax-highlighted git diffs (binary: delta)
wireguard-tools        # wg-quick — used by vpns() / exitnode() in shell.local.sh
azure-cli              # Azure CLI (binary: az) — official extra-repo pkg, so here not packages.aur.txt (unlike gcloud)

# kubectl + the GKE auth plugin must share /usr/bin (pacman, not mise) so kubectl
# can invoke the plugin by name in every shell. [F-APP-CHANNELS]
kubectl

# Selected Omarchy defaults removed by "Remove Preinstalled"; restore them
# through pacman, not alternate launchers or self-managed installers. [D-PKG-REMOVE][F-APP-CHANNELS]
opencode               # OpenCode; install seeds Omarchy config only when missing.
obsidian               # workspace 3 — notes
discord                # chat (native client; omarchy default is a webapp — we want full voice/screen-share)

# Claude Desktop Cowork sandbox: an app-managed ephemeral QEMU/KVM microVM for
# running agent code in isolation. Enabled by default; KVM is already usable here
# (/dev/kvm is world-rw). The app owns the VM lifecycle — we only supply the tools. [F-APP-CHANNELS]
qemu-base              # headless QEMU + KVM emulator (pulls virtiofsd for the host↔VM folder share)
edk2-ovmf              # OVMF UEFI firmware the Cowork VM boots through
