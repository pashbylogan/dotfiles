# Omarchy builtins to remove; processed by `install` via `omarchy pkg drop`.
# Audit ~/.local/share/omarchy/ before adding — some "builtins" back omarchy
# commands (full rule + examples in decisions.html#D-PKG-REMOVE). [D-PKG-REMOVE]

