# Unwanted Omarchy packages; processed by `install` via `omarchy pkg drop`.
# Quattro may reinstall base packages during a major migration, so this file is
# the persistent desired-state deny-list. [D-PKG-REMOVE]

# bash/aliases guards `cd` with `command -v` — coreutil fallback kicks in.
zoxide

# inxi remains because omarchy-debug uses it directly.
lazygit
plocate
tldr

# ruby ↔ rubygems mutual dep — both must be in the same pacman -Rns transaction.
dotnet-runtime
mariadb-libs
ruby
rubygems
tobi-try

# Quattro base applications this machine deliberately does not keep.
aether
cliamp
herdr
kdenlive
lazydocker
libreoffice-fresh
moonlight-qt
obs-studio
omacut
omawrite
pinta
tensaku
xournalpp

# Replaced by Quattro's stock web-app launcher. Keeping the old native package
# would create two Discord application identities. [D-PKG-REMOVE][D-WEBAPP]
discord

# User-removed browsers (not omarchy builtins, so no ~/.local/share/omarchy/
# audit applies). Brave Origin is the sole browser. [D-PKG-REMOVE]
chromium
zen-browser-bin

# Replaced by ghostty as the default terminal; install sets the default before
# this drop. Base-package/reinstall caveat: decisions.html#D-TERM-GHOSTTY.
# [D-TERM-GHOSTTY]
foot
alacritty
