# AUR-only packages installed by `install`; prefer packages.txt when available.
# Same syntax as packages.txt. [D-SCRIPTS-MINIMAL][F-CLI]

slack-desktop          # workspace 3 — AUR-only on Arch (no official-repo package)
spotify                # workspace 10 — matches omarchy-base.packages (omarchy's chosen Spotify path is AUR) [F-APP-CHANNELS]
# JetBrains Toolbox owns IDE updates/version pinning better than AUR IDE packages.
jetbrains-toolbox
postman-bin            # API client — AUR-only on Arch (official prebuilt binary; no repo/omarchy pkg)
claude-desktop         # Claude Desktop — installs Anthropic's official Linux .deb verbatim (committed checksum matches the signed InRelease); no repo/omarchy pkg [F-APP-CHANNELS]

# gcloud core CLI (no official-repo pkg); used by gssh() in shell.local.sh.
# Ships /etc/profile.d/google-cloud-cli.sh, so gcloud lands on PATH natively.
google-cloud-cli
# GKE auth plugin kubectl invokes by name (binary in /usr/bin); split of the
# google-cloud-cli base above, whose credentials it uses. [F-APP-CHANNELS]
google-cloud-cli-component-gke-gcloud-auth-plugin
